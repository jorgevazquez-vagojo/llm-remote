services:
  llm-remote:
    build: .
    container_name: llm-remote-jorge
    restart: unless-stopped
    env_file: .env
    volumes:
      - llm-data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  llm-data:
